# SL Identity Core 1.5

Questa versione stabilizza il motore interno.

## Cosa contiene
- `profile.json` con struttura coerente
- `index.html` pulito
- `js/app.js` con fallback e gestione errori
- `css/styles.css` ripulito
- `sw.js` aggiornato per evitare vecchie cache

## Obiettivo
Preparare SL Identity al Builder visuale e ai temi configurabili.
